# Work record

## 2026-09-19 — robots false-blocker correction

Owner approved correcting false blockers after distinguishing robots unavailability from explicit restrictions. The previous blanket stop on robots HTTP 403 was a conservative implementation choice, not an explicit Disallow. Supersedes that part of the earlier fail-closed policy: RFC 9309 section 2.3.1.3 permits a public content request after robots 4xx. Apply that rule except 429 (rate limiting still stops). Content 401/403/429, challenges, explicit Disallow, unresolved HTML policies, network/server errors, TLS and host restrictions remain enforced. No authentication, proxy rotation or browser impersonation is introduced.

Regression reproduced the old stop for robots 400/401/403/418 before implementation. All 100 tests pass after correction, including content-level denial, rate limits, network errors and existing explicit restriction cases. Error messages no longer double-prefix robots failures and distinguish unexpected HTML. Hosted results are not yet verified at this checkpoint; metadata/state history are unchanged by this code edit.

## 2026-09-19 — implementation authorized

Owner approved RSS plus JSON, review-only GRC integration, extensible government URLs, automatic hosting and collection, and cloud maintenance. The latest instruction expands the seed inventory to all GRC regulator watch lists.

Read current enforcement, GR/GA upstream, legislative registries and cloud monitoring lane descriptions. Import only public source identities/URLs and classifications; exclude private destinations, project priorities, credentials, captures and legal determinations. Prior source status is discovery evidence, not verification of this new collector. The fixed-instrument practice registry is reference material, not a changing regulator listing.

Plan amendment: preserve the original design as history. Replace its initial 14-source limit with the reconciled watch inventory. Exact API URLs are retained pending tested adapters; additional institutions named without listing URLs remain visible pending scopes. Use complete JSON event history and configurable RSS windows (default 500); RSS is not a complete archive once that window is exceeded. Event identity includes a source-specific monotonic revision so A-B-A reversions are distinct.

Local repository is independent. No existing GRC automation or canonical store is modified. Shared brain routing was inspected; source decisions use current local project records. GitHub CLI authentication verified outside the restricted network sandbox; the target repository does not yet exist. GitHub connector currently exposes no repositories.

Implementation, tests, live validation and remote deployment outcomes will be appended below.

### First live probe and correction

Python 3.12.14: 26 tests passed. Bounded probes collected TCCT ruling links and explicit dates; two PDFs returned valid signatures and SHA-256 fingerprints. ETDA robots exceeded the response bound; TISI failed TLS validation; BOT listing returned no article links. OCPB's generic listing returned navigation links, exposed by representative inspection. The uncommitted probe state was preserved outside the public repository before resetting the draft production baseline. Parser now excludes navigation/footer/sitemap elements, TCCT has an observed .detail/.text-red2 selector fixture, and OCPB generic index plus BOT are pending. No contaminated draft observations were published. This is a pre-release qualification correction, not deletion of published history.

### Release qualification

Implemented 96 public source scopes across 53 institution groups, RSS/JSON contracts, dashboard, offline builds, hourly workflow, cloud onboarding and source-request template. Live local qualification retained 475 baseline observations from 7 successful scopes; 17 enabled scopes failed, 8 were blocked and 64 remained pending. No claim of complete regulator coverage. Date parsing now preserves RFC timestamps and explicit Thai/BE dates. Added fail-closed missing-state behavior and first-PDF-fingerprint semantics: establishing a hash does not imply a document change. Latest fingerprints are available in current item observations; immutable event evidence is not rewritten. 35 tests passed. See the verification record for release evidence and next owner actions.

### Public deployment and cloud readback

Created the owner-approved public GitHub repository, enabled Pages with workflow deployment, set its public base URL and enabled the hourly collector. First GitHub run 35432350248 passed collection and deployment; its separate health job failed for real source limitations. Retained output increased to 575 baseline items, including TCCT press releases. PDPC became access-blocked from GitHub; prior observations were preserved. HTTPS RSS/JSON/OPML readback passed, as did Linux CI and desktop/mobile browser checks. Direct GitHub connector file reads and repository permissions verified; account-specific Codex environment selection remains an owner setup step. No existing GRC tasks, canonical stores or private integrations were changed.

Next obligations: maintainers qualify failed/pending source scopes with representative fixtures and permitted live checks; owner selects the repository in Codex cloud for coding tasks and chooses where existing GRC consumers should read these public endpoints. Those downstream changes are outside this producer deployment.

Cloud/local artifact comparison exposed RSS extension ordering differences after persisted-state reload. Added a regression and sorted extension fields; 36 tests passed. This changes serialization order only, preserving all event identities and source evidence. Re-running the hosted workflow verifies the amended producer and deployment.

Final health regression found that repeated attachment failures could emit a temporary healthy transition within each run. Collector now sets final health only once after document checks. The failing regression passed after the fix; 37 tests passed. This prevents misleading recovery notices without weakening the separate health-failure signal.

Second hosted run 35432691007 passed collection/deployment and retained the same 575 event IDs and identical combined RSS bytes, with no false content-change events. Health remained red for unresolved source coverage. Public readback passed again. The final health-transition correction is test-verified and queued for the next scheduled/manual run; no claim is made that that later code path ran in this second collection.

## 2026-09-19 — full source remediation requested

Owner requested repair of all 25 failed/blocked and 64 pending scopes using alternative methods. Work is isolated on fix/source-coverage. Investigate each source family against live official evidence; add tested public API/RSS/HTML or explicitly limited selected-content monitoring where appropriate. Access controls, robots, TLS and evidence gates remain intact. Source IDs, retained observations and existing GRC processes must be preserved. Parallel workers own disjoint source-qualification files, not shared production state. Integration and hosted verification remain with the main maintainer.

Qualification introduced explicit public JSON, JSON-in-HTML, inert Next data and selected-field snapshot adapters; corrected selectors and official destinations; validated AMLO's exact public intro form; and repaired missing TISI/OCS intermediate chains without adding trust roots or disabling verification. First integrated scratch-state run: 67 healthy, 5 blocked, 1 failed among 73 enabled scopes; all 575 prior item/event IDs preserved, 1,642 items and 1,932 events in scratch outputs. This is local evidence, not hosted verification. Source limitations and all scoped outcomes are recorded in `docs/qualification/`.

Review exposed and corrected OCPB view-counter revision noise, relative TLS paths with CLI `--root`, repeated hashing of large NCSA payloads, and the standard-library robots parser's incorrect literal `/?` normalization. Protego 0.6.2 replaces that parser, with regressions proving genuine query/download and wildcard restrictions remain enforced. Unavailable/HTML robots, denied HTTP, unsafe/self-signed certificates and application errors still fail closed. OCS's exact public SearchLaw queries returned business error PSL_6013, so the search scopes remain pending despite permitted transport; OCS law-news coverage is explicitly partial.

During remediation the owner additionally requested newest-first feeds and visible source metadata. Presentation now distinguishes source publication/update dates from observation time; unknown publication dates remain unknown. TCCT sections receive distinct labels and source-scope types/topics, issue numbers are extracted only from explicit titles, and short official excerpts are retained where available. Immutable event history is not rewritten for display enrichment. A separate current-item view is being verified alongside event-history feeds.

Final pre-publication validation:98 tests pass; registry73 enabled/23 pending; deterministic offline generation of250 files and recursive XML/JSON checks pass. Second full local scratch collection:68 healthy/4 blocked/1 failed,1,651 items/2,039 events, all575 prior IDs retained. Independent source/renderer review reported no remaining important findings. Headless Edge desktop1440 and mobile390 checks passed,96 source rows,10 PDPC-filtered rows, no page errors or horizontal page overflow; screenshots inspected. Production state/site are still untouched at this gate. Metadata enrichment may create UPDATED observations; the reader guide distinguishes them from proven source-document amendments.

Maintenance PR1 opened. A fixture-only whitespace normalization inadvertently replaced a Unicode U+2028 inside one JSON string with a literal newline; the focused/full suite and PR CI caught the invalid JSON. The fixture was corrected to the equivalent escaped `\u2028`, the formatter was narrowed to real newline characters, and98 tests passed again before the correction was pushed. No production state or collection ran from the failing fixture commit. This failure and correction are retained in branch history.

PR1 merged as54b5a77 after final Linux CI35435439296 passed. Hosted run35435485662 completed collection in6m17s and Pages deployment successfully; the separate source-health job failed as intended for real restrictions. Public HTTPS readback validated RSS/JSON contracts, metadata, current-feed deduplication, all575 original item/event IDs, and review-only invariants. Hosted result:52 healthy,21 blocked,23 pending;1,464 items and1,831 events. All repaired TCCT sections are operational. Some sources that passed locally are denied/timed out from GitHub, so local68 healthy is not represented as cloud coverage. Remaining44 scopes require permitted official endpoints, publisher access/service repairs, or an owner-approved supported execution arrangement; no bypass or existing GRC-system mutation occurred. See the appended hosted evidence in `reports/SOURCE_REMEDIATION.md`.
