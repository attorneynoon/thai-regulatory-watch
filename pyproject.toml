[project]
name = "thai-regulatory-watch"
version = "1.0.0"
requires-python = ">=3.12"
description = "Public regulatory observations, RSS and review-only GRC intake"
[tool.ruff]
target-version = "py312"
